CREATE TABLE "A_XML_EXAMPLE_APPFORM1"
  (
    "ID"   NUMBER(14,0) NOT NULL ENABLE,
    "TYPE" VARCHAR2(80 BYTE) NOT NULL ENABLE,
    "CREATESTAMP" DATE NOT NULL ENABLE,
    "UPDATESTAMP" DATE NOT NULL ENABLE,
    "UPDATECOUNT" NUMBER(12,0) NOT NULL ENABLE,
    "CREATOR"     VARCHAR2(60 BYTE) NOT NULL ENABLE,
    "UPDATEDBY"     VARCHAR2(60 BYTE) NOT NULL ENABLE,
    "STATE"     VARCHAR2(60 BYTE) NOT NULL ENABLE,
    "XML_CONTENT" "SYS"."XMLTYPE" ,
    CONSTRAINT "A_XML_EXAMPLE_APPFORM1_PK" PRIMARY KEY ("ID") ENABLE
 );
 /